/**
 * Module `plugins` — point d'entrée minimal du plugin system (ADR-002) :
 * pas d'écosystème au MVP.
 *
 * Stub lot 0 : aucun rendu, aucune logique de chargement.
 */
import type { DenContext } from "../core/registry";

export function init(_ctx: DenContext): void {
  // Lot dédié : point d'extension minimal, pas d'écosystème au MVP (ADR-002).
}
