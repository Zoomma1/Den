# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sidecar (Node)
sidecar/node_modules
sidecar/dist

# Rust / Tauri
src-tauri/target
src-tauri/gen/schemas

# Workspace utilisateur (projets, layout) en dev — jamais versionné. Ancré à
# la racine : un fixture nommé workspace.json ailleurs dans l'arbre reste
# versionnable. Couvre aussi .tmp / .bak (écriture atomique + sauvegarde).
/workspace.json*
